//pragma solidity ^0.4.24;

// solscan-disable-next-line all
contract naming {

    // solscan-disable-next-line naming-convention
    struct test {
        uint a;
    }


    // solscan-disable-next-line pragma,all
    struct test2 {
        uint a;
    }

    // solscan-disable-next-line wrong-naming-convention
    struct test3 {
        uint a;
    }
 
}
